{
  "name": "with-vuetify",
  "dependencies": {
    "nuxt": "latest",
    "vuetify": "latest",
    "snyk": "^1.192.3"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "snyk-protect": "snyk protect",
    "prepublish": "npm run snyk-protect"
  },
  "devDependencies": {
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.1"
  },
  "snyk": true
}
